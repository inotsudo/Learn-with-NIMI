// Shared constants for the referral system.
// Imported by both client pages and API routes so key changes propagate everywhere.

export const PENDING_REF_KEY = "nimipiko_pending_ref";
export const REFERRAL_CODE_LENGTH = 8;
